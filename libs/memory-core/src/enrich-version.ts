/**
 * Semver of the enrichment algorithm. Written into node.enrich_ver.pass on every
 * enrichment pass. A breaking change to the enrichment algorithm increments the
 * major version and triggers re-enrichment detection.
 */
export const ENRICH_VERSION = '1.0.0';
