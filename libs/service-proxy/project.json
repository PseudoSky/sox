{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "service-proxy",
  "projectType": "library",
  "root": "libs/service-proxy",
  "sourceRoot": "libs/service-proxy/src",
  "tags": [
    "type:lib",
    "area:platform",
    "group:runtime"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{workspaceRoot}/libs/service-proxy/dist"
      ],
      "options": {
        "command": "tsc --project libs/service-proxy/tsconfig.lib.json && echo '{\"type\":\"commonjs\"}' > libs/service-proxy/dist/package.json",
        "cwd": "."
      },
      "cache": true,
      "inputs": [
        "{projectRoot}/src/**/*.ts",
        "{projectRoot}/tsconfig.lib.json"
      ]
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --config libs/service-proxy/vitest.config.ts",
        "cwd": "."
      },
      "cache": true,
      "inputs": [
        "default",
        "^production"
      ]
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": [
          "libs/service-proxy/**/*.ts"
        ]
      }
    }
  }
}
