{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "install-engine",
  "projectType": "library",
  "root": "libs/install-engine",
  "sourceRoot": "libs/install-engine/src",
  "tags": ["type:lib"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/libs/install-engine/dist"],
      "options": {
        "command": "tsc --project libs/install-engine/tsconfig.lib.json && echo '{\"type\":\"commonjs\"}' > libs/install-engine/dist/package.json",
        "cwd": "."
      },
      "cache": true,
      "inputs": [
        "{projectRoot}/src/**/*.ts",
        "{projectRoot}/tsconfig.lib.json"
      ]
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --config libs/install-engine/vitest.config.ts",
        "cwd": "."
      },
      "cache": true,
      "inputs": ["{projectRoot}/src/**/*.ts"]
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["libs/install-engine/**/*.ts"]
      }
    }
  }
}
