{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "tokenguard-core",
  "projectType": "library",
  "root": "libs/tokenguard-core",
  "sourceRoot": "libs/tokenguard-core/src",
  "tags": [
    "type:lib",
    "area:shared",
    "group:codec"
  ],
  "targets": {
    "build": {
      "executor": "@adhd/sox-nx:atomic-tsc",
      "outputs": [
        "{workspaceRoot}/libs/tokenguard-core/dist"
      ],
      "options": {
        "main": "libs/tokenguard-core/src/index.ts",
        "outputPath": "libs/tokenguard-core/dist",
        "tsConfig": "libs/tokenguard-core/tsconfig.lib.json",
        "assets": [],
        "clean": true,
        "rootDir": "libs/tokenguard-core/src"
      },
      "cache": true,
      "inputs": [
        "{projectRoot}/src/**/*.ts",
        "{projectRoot}/tsconfig.lib.json"
      ]
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --config libs/tokenguard-core/vitest.config.ts",
        "cwd": "."
      },
      "cache": true,
      "inputs": [
        "default",
        "^production"
      ]
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": [
          "libs/tokenguard-core/**/*.ts"
        ]
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p libs/tokenguard-core/tsconfig.json --noEmit",
        "cwd": "."
      },
      "cache": true,
      "inputs": [
        "default",
        "^production"
      ]
    }
  }
}
