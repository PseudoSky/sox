{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "host-registry",
  "projectType": "library",
  "root": "libs/host-registry",
  "sourceRoot": "libs/host-registry/src",
  "tags": ["type:lib"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/libs/host-registry/dist"],
      "options": {
        "command": "tsc --project libs/host-registry/tsconfig.lib.json && echo '{\"type\":\"commonjs\"}' > libs/host-registry/dist/package.json",
        "cwd": "."
      },
      "cache": true,
      "inputs": [
        "{projectRoot}/src/**/*.ts",
        "{projectRoot}/tsconfig.lib.json"
      ]
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --config libs/host-registry/vitest.config.ts",
        "cwd": "."
      },
      "cache": true,
      "inputs": ["default", "^production"]
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["libs/host-registry/**/*.ts"]
      }
    }
  }
}
