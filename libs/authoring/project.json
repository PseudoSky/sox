{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "authoring",
  "projectType": "library",
  "root": "libs/authoring",
  "sourceRoot": "libs/authoring/src",
  "tags": [
    "type:lib",
    "area:platform",
    "group:authoring"
  ],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": [
        "{workspaceRoot}/libs/authoring/dist"
      ],
      "options": {
        "main": "libs/authoring/src/index.ts",
        "outputPath": "libs/authoring/dist",
        "tsConfig": "libs/authoring/tsconfig.lib.json",
        "assets": [],
        "clean": true,
        "rootDir": "libs/authoring/src"
      },
      "cache": true,
      "inputs": [
        "{projectRoot}/src/**/*.ts",
        "{projectRoot}/tsconfig.lib.json"
      ]
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --config libs/authoring/vitest.config.ts",
        "cwd": "."
      },
      "cache": true,
      "inputs": [
        "default",
        "^production"
      ]
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": [
          "libs/authoring/**/*.ts"
        ]
      }
    }
  }
}
