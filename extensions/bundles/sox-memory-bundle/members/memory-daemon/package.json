{
  "name": "@adhd/sox-extension-memory-daemon",
  "version": "0.2.1",
  "description": "Supervised Unix-socket daemon that drains the enrichment queue and runs deterministic batch enrichment via memory-core",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit --project tsconfig.json",
    "test": "vitest run"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0",
    "sqlite-vec": "^0.1.9",
    "fastembed": "^2.1.0",
    "onnxruntime-node": "1.21.0"
  }
}
