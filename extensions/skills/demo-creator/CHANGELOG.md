# Changelog

## 0.1.0

- Initial release. Ports the `demo-creator` skill into a born-conformant
  `skill`-type extension: declarative runtime, `SKILL.md` entrypoint, bundled
  `assets/DEMO.template.md` (authoring rules R1–R10 + Definition of Done) and
  `scripts/validate_demo.py` (deterministic pre-emit gate).
