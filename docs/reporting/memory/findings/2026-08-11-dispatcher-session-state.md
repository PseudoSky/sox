# Dispatcher session state — 2026-08-11 (resume after compaction)

> Written because the live memory server is DOWN (stale WAL-index sidecar, BL-373) and the MCP
> memory write failed with `E_IO` — this file is the durable resume point. Restore to memory once
> the server recovers.

## Main HEAD: `04414dca`

Merged this session (all reviewed, gated, worktrees pruned):
- `582b41d0` release-train (store-adapter 0.4.0, memory-core 0.7.0)
- `21f62382` sqlite-soft-deps (BL-498/499/461)
- `76af069b` bl497 (BL-497 floor env-tunable, default 20)
- `c18a03ba` bl202 (BL-202 flake)
- `1c1c902f` A2 store-adapter (FEAT-SOXGRAPH-001)
- `53d215a3` graph-store A2 + recursive-CTE fallback (published 0.8.1)
- `d5296b3b` DEBT-SOXGRAPH-001 (FTS delegation + F1 agent_id fix BL-502)
- `fe7df71a` catch-trace (empty-catch rule + ~35 traces + BL-500 formatToolError fix)
- `d369fe5b` smoke-gate (Build-first preflight)
- `2844be13` BL-504 (NOT INDEXED rowid-PK, 57s→40ms)
- `04414dca` BL-505 (t_created,rowid tiebreak)

## Published npm

- `@adhd/sox-store-adapter@0.5.1` (0.5.0 deprecated — workspace:* broken publish)
- `@adhd/sox-graph-store@0.8.1` (0.8.0 + head-parity patch)
- adhd relocked to these Aug 10; backlog CLI installed dist is current.

## Live incidents (owner decisions pending)

0. **RESOLVED 2026-08-11 — Memory server RESTORED (BL-373 third recurrence).** Renamed `~/.memory/memory.db-tshm` → `memory.db-tshm.stale-2026-08-11-recovery-20260811-163017` (never deleted), killed orphaned backend + respawning adhd-side proxies (83087/84637/40788), bootstrapped launchd unit, verified `memory_ping` store block healthy on pid 70323. **Runtime change: unit now runs Homebrew node 26.5.1 (was nvm 24.11.1)** — `soxe service enable` refused volatile nvm pin; re-enabled with `--node-path=/opt/homebrew/Cellar/node/26.5.1/bin/node`, plist content-hash c4b09491; owner must accept. `status:'degraded'` on ping is the PRE-fix bundle's embed-only derivation (store block healthy); ping-honesty fix (unmerged) makes it truthful. Memory MCP writes now succeed (episode 01KZSBVZ8C5ANVPVYH7HA40X13). Six tooling/ops gaps documented in `findings/2026-08-11-memory-server-recovery-confusions.md` (committed cb32e7c7): no `soxe service stop`; bootout exits 0 but backend survives; adhd-side serve proxies respawn backends (sweep missed them); status reports live pid for unloaded unit; enable's volatile-node guard forced runtime change; zsh PPID readonly.
1. **Memory read path — recall-0 / entity timeouts:** debugger root-caused as BL-373 stale sidecar (now fixed + restored) + SEPARATE algorithmic defect: memory_list_entities N+1 (4,238 entities × ~23.3ms getEdges ≈ 98.7s > 60s MCP timeout) — needs single-query join rewrite. Original recall-0 claim NOT reproducible on current backend (reporter pid 88179 died Aug 9); re-verify before filing.
2. **Embed pipeline stalled:** embeds_completed=6 / coverage 12.6% figures were from the dead instance (server counters now 30/0.694); 3.4-billion-ms timestamps are REAL episode ages, not corruption. Re-assess post-recovery.
3. **Backlog graph read-only:** create-item/writeNode fails `foreign key mismatch referencing node` — Defect B (explicit-rowid FK), C-fix implemented, D unblocks once published + relocked.
4. **Data-loss triage in flight (debug task ses_00d45e537ffemkTzEkX6iR40LC):** mixed-engine writer (better-sqlite3) root cause confirmed by repro; Aug-11 19:31:29 stock-SQLite writer culprit UNVERIFIED; recoverability assessment pending.

## Root-cause clusters

- **Cluster 1 (plausibly the A2 weave):** BOTH `~/.memory/memory.db` AND `~/.adhd/backlog/production/data/backlog.db` carry the SAME malformed Turso FTS object `__turso_internal_fts_dir_idx_fts_node_key` (sqlite3: `malformed database schema near "USING": syntax error`). Triage episode `01KZS4PTQ9GC8BM34CM7475024`: fresh stores reproduce via `openGraphBacklogStore` → `ensureFtsIndex` emitting `CREATE INDEX ... USING fts` on turso + adapter `exec()` silent no-op (BUG-TURSO-ADAPTER-EXEC-DDL-SILENT-NOOP-001 OPEN; INVESTIGATION-FTS-TRIGGER-TURSO-UNRESOLVED-001 OPEN). C-fix agent in flight confirms/refutes.
- **Cluster 2 (not our code):** adhd hand-authored `edge` DDL `REFERENCES node(rowid)` created 2026-07-11 by Drizzle migration `0000_sad_onslaught.sql` (now missing from repo). libsql 0.7.2 rejects at DML-prepare when `PRAGMA foreign_keys=ON`; better-sqlite3 accepts.
- **Cluster 3:** memory read path runs the PRE-weave bundle `6eff093e3991` (weave merged but NOT deployed). recall-0 likely stalled-embed/vector or deployed-bundle (PERF-003/BL-496/BUG-008), not A2.

## In-flight agents (at time of writing)

- (a) **C-fix** (typescript, `fix/c-fts-object`): fresh-store repro, malformed-object root cause, store-adapter vs graph-store release path.
- (b) **memory read-path incident debugger** (debug).
- (c) **DEBT-SOXGRAPH-002 reviewer** (branch `fix/debt-soxgraph-002` c29fa2b4 — openDb FTS setup delegates to adapter.ensureFtsIndex, 670/0 green; incident-angle: does it worsen memory's working path / implicate fts-ops.ts?).

## Correct sequencing (user directive: C then D through C's fix)

Fix the CODE (ensureFtsIndex/exec-noop malformed-object source) FIRST → then DEBT-SOXGRAPH-002 can merge safely → then D: backlog recovers through fixed code (fresh create-item proven green); any residual live-DB corruption repaired by fixed code path, NOT raw SQL. Live-DB repair decision fully parked pending C-fix + reviewer confirmation.

## Architect migration design (delivered, NOT dispatched)

`_schema_migrations` ledger table + store-adapter machinery (runMigrations, user_version fast-path, DML FK preflight probe, adapter-shaped backup since VACUUM unavailable on turso 0.7.x) + graph-store MIGRATIONS list + consumers call ONE entrypoint (additive auto at open, destructive operator-gated per ADR-0010 D3/BL-313) + drizzle coexistence rule (library owns domain DDL; drizzle only app tables; adhd must stop authoring node/edge DDL) + FK-form DDL lint. Phases: store-adapter 0.6.x → graph-store 0.9.x → memory-core → docs/ADR → live repair. ADR-0008 backfill never executed — graph-store still raw DDL strings.

## Deferred / open

- BL-503: stale `extensions/agents/researcher` lockfile importer (pnpm-lock.yaml `M` on main; separate commit when that work lands)
- DEBT-SOXGRAPH-002 / BL-504 / BL-505 resolutions pending graph writable
- BL-490: memory-core typecheck-tests pre-existing red (8 errors: bl472-embed-drain-e2e.spec.ts:178,206; enrich.spec.ts:863,891,1093; telemetry.spec.ts:372,373,376)
- 2 LOW smoke-gate findings: external-exports `/` false-positive guard; attw concurrency race
- M1: sqlite `fts_node` hardcode
- Companion F1 test vacuous on empty path
- memory-server bundle stale re: formatToolError fix (rebuild+deploy is owner call)
- BL-500 fixed but not resolved (needs red→green test)

## Agent landing log (compacted session)

- **2026-08-11 — OWNER MANDATE (merge gate): "I want this code reviewed before merging."** Applies to the STRIP+PURGE changes (bl373 worktree, in flight) — must get an independent review verdict BEFORE the strip branch merges to main. Also the alignment note (BackupConfig skeleton) was delivered mid-flight to the strip agent via opencode builtin task_id (ses_00d854300ffepLClHxbz6dEjsQ) — the agent must state in its final report whether it applied it. REVIEW SCOPE at strip landing: (1) the full strip+purge diff (deleted env vars + recoverTruncatedWal removal + any BackupConfig skeleton added); (2) verify each deleted var actually has zero remaining references (grep the whole repo, incl. docs/ADR inventory, .plist files, READMEs); (3) verify no behavior toggle was silently converted into a config switch that can still disable a safety path (backup/repair/verify must be un-disablable); (4) ADR-0013 inventory dispositions match actual deletions. Dispatch reviewer on the worktree diff once the strip task completes, before combined-staging merge. This is a HARD pre-merge gate — do not merge the strip branch without the review verdict recorded in the ledger.
- **2026-08-11 — BACKUP DESIGN LANDED (task ses_00d29a0d8ffekYHq773DUsbwOJ, architect, READ-ONLY): three layers, ONE mechanism, ADR-0013-compliant.** (1) **Pre-service-start backup**: bundle-level hook in memory-server — `preStartBackup(dbPath)` in `runBackend()` (backend mode) and `serve()` init (direct-stdio) BEFORE first request can open store (store opens lazily on first tool call, so pre-start = pre-first-request); idempotent via existing `.auto-backup-<pathHash>` mtime marker (a burst of restarts with no writes ⇒ one backup; writes ⇒ fresh); naming `memory-<ISO>.db` unchanged. Failure mode = serve-with-loud-warning + health flag (`backup.last_status:'failed'` + `last_error` on memory_ping/memory_stats), NEVER refuse-to-serve (availability > backup). (2) **Scheduled backups (mem-server case)**: in-process self-rescheduling `setTimeout` chain (never setInterval) modeled line-for-line on the proven enrich-tick pattern (index.ts:2546-2614 — in-flight flag, skip counter, unref), default `backup.intervalMs` 6h; pre-start backup IS the first tick, timer armed at `now+intervalMs` after it settles (no overlap by construction). Rationale vs external launchd StartInterval: single config surface + in-process health visibility + reentrancy lesson already paid. (3) **Non-server-mode**: existing `memory-cli backup` (memory-cli/src/index.ts:475-520, :594) extended — `--dest` optional defaulting to `backup.dir` (`~/.memory/backups`), retention prune after success; PLUS opt-in `backupOnOpen?: boolean` on memory-core `openDb` (default OFF for library consumers, ON at sox CLI entrypoints via shared helper; backlog CLI opts in explicitly). Rejected: adapter-level (`AdapterConfig`/`createStoreAdapter`) — data-layer boundary (`data→memory-core` forbidden) + would surprise every consumer. **MECHANISM = VACUUM INTO on a separate read-only connection — NO CHANGE, existing `backupStore` already correct** (backup.ts:125-293, opens readonly via createStoreAdapter then `adapter.backupTo` → `VACUUM INTO`; turso-adapter.ts:601). Researcher verified (sqlite docs + vacuum.c): VACUUM INTO opens a READ transaction (`wrflag=0`), WAL writers unaffected, committed-but-uncheckpointed WAL frames INCLUDED (readers check WAL first), output = consistent snapshot, `application_id` PRESERVED, standalone DELETE-journal file with no -wal/-shm companion. Only practical TS path: Online Backup API unavailable — `@libsql/client` exposes no backup method, native binding `backup()` "currently not supported". Rejected WAL-preserving fs copy (correct only under write-lock quiescing protocol; fragile). **RETENTION (new):** prune oldest-first in `backup.dir`, keep `backup.retentionCount` default **24** files matching `memory-*.db` glob ONLY (never dotfiles/foreign); ≈6 days at 6h cadence, 22h incident gap covered 4× in day one. **CONFIG SHAPE** (new `libs/memory-core/src/config.ts`): `BackupConfig { enabled: true; intervalMs: 6*60*60*1000; retentionCount: 24; dir }`; `dir` resolution: config.backup.dir → `SOX_AUTO_BACKUP_DIR` (host config, KEPT per ADR-0013 D5) → `~/.memory/backups`. NO `SOX_BACKUP_*` toggle EVER (enabled is a typed `true`, not a switch — D3 numeric tuning only). **ENGINE-SAFETY:** backup path never mixed-engine writer (`backupStore` resolves engine via STORE_ADAPTER, uses only adapter.backupTo — no unwrap, no better-sqlite3 on turso store; BL-385's point). Post-engine-guard-merge addition: assert `PRAGMA application_id` on copy === source (extends BL-449 probes) + pre-open engine detection (preflightSchemaSanity pattern) so STORE_ADAPTER=sqlite misconfig fails loudly instead of cross-engine backup. **IMPLEMENTATION SEQUENCING (binding):** 1) strip task (bl373) merges FIRST — deletes `SOX_AUTO_BACKUP_ENABLED` (backup.ts:342) + repair/verify toggles, introduces `backup.enabled` typed skeleton; same file, feature must not touch backup.ts until then; 2) engine-guard merges before/with (app_id assertion depends); 3) this feature lands on merged staging, NO nx build memory-server needed at impl time. **GAPS (honest):** exact serve() init hook line direct-stdio (index.ts ~3273-3283 — implementer reads once); encrypted-store backup path (backup.ts:196 does NOT forward encryption — thread-through vs explicit E_IO refusal, open decision); verify-step readonly reopen may leave inert .tshm/-wal ON THE COPY (glob-scoped prune is the guard; restore .db alone — DELETE-journal); libsql embeds SQLite 3.43.0 predating WAL-reset fix ≥3.51.3 (concurrent-multiprocess-WAL trigger; pin fixed build separately, `integrity_check` on copy is in-repo safety net); backlog CLI (adhd repo) is contract-only from this repo — sox side = `memory-cli backup` + `backupOnOpen`. Tests suggested naming BL-475. No files written; depends on strip landing.
- **2026-08-11 — ENGINE-GUARD IMPLEMENTATION LANDED (task ses_00d4595a7ffehmfqZTK3hqmAgp, commit 51fc0247 on fix/engine-guard, 19 files +1535/−52, off main@04414dca, BL-508 named, tree DIRTY only with own 19 files).** Marker: application_id 0x534F5854 'SOXT' (turso) / 0x534F5853 'SOXS' (sqlite); _sox_engine row (engine, sox_version=PKG_VERSION 0.5.1, driver_version, first/last_opened_at); better-sqlite3 PRAGMA is PRIMARY probe (WAL-aware — fs header byte-68 read is stale in uncheckpointed-WAL window, documented degraded fallback); fs probe used for turso-side pre-driver refusal. Files: engine-guard.ts (assertStoreEngine/assertStoreEngineSync, getEngineIdentity, ensureEngineMarker, warnOnEngineVersionMismatch, EEngineUnmarked); errors.ts:43-141 E-TursoNativeStore extended + new E-SqliteNativeStore; turso-adapter.ts:484-498 sqlite-marker refusal BEFORE driver opens + ensureEngineMarker :565; sqlite-adapter.ts:174-218 tooling-intent probe before writable handle + marker write init :264; factory.ts allowForeignEngine wired to dormant migrateOnAdapterChange; preflight.ts repair-intent proceeds + records detected_engine; migration.ts _sox_engine in SKIP_TABLES; memory-core db.ts repair-intent engine recording + engine_identity in store.open.finish + getStoreEngineIdentity export; graph-store index.ts engineIdentity getter (lazy per-adapter, guard fails closed on sqlite marker for turso) + eager touch in createGraphBackend; bundle index.ts:1126 store_engine: await getStoreEngineIdentity(adapter) in ping store block (data not health dimension); eslint rule cited exception. Tests red→green BL-225 named BL-508: RED pre-change 5/7 (better-sqlite3 tooling open silently succeeded; no _sox_engine table), GREEN store-adapter 418/418, memory-core 668+8skipped, graph-store 117/117; lint+typecheck green all; memory-server typecheck green only (deploy guard). DIVERGENCES (architect review): ping-health.ts + openGraphBacklogStore don't exist on main (they're on the weave branches) — ping store_engine wired in bundle index.ts directly; graph-store guard at createGraphBackend (debt-soxgraph-002 backlog-store open inherits via engineIdentity); legacy backfill refined (ambiguous legacy stays unmarked); graph-store dep ^0.5.0→workspace:* + relock (was resolving .pnpm published snapshot lacking new exports — lock diff exactly that edge); removed pre-existing unused getClusterMetrics import in bundle (failing typecheck even on main). UNVERIFIED: store_engine verified by typecheck + memory-core unit test only (gate); GitNexus unavailable (CLI detect-changes reads main's dirty docs/research/content-first/proxy tree); smoke not run (merge gate). BL-508 NOT filed (graph FK incident) — named, must file when graph writable. Backlog graph max verified = BL-505 (442 items).
- **2026-08-11 — OWNER BINDING DIRECTIVE #4 (backup strategy): "I want pre service start backups (supports non server mode store usage) + scheduled for the mem server case."** Direct response to the triage finding (no backup between Aug 10 19:50:48Z and recovery ~22h — auto-backup only ran on clean server start). Requirements: (a) PRE-SERVICE-START backup — before the memory-server service starts accepting requests, capture the store (known-good start point for every service session); (b) SCHEDULED backups for the long-running memory-server case (periodic, not just at start — the 22h gap cannot recur); (c) NON-SERVER-MODE store usage must also get backups (direct store use via CLI/tools/batch, not just the server — covers backlog CLI + sox tooling). Must respect ADR-0013: schedule/behavior is typed config, never env toggles; SOX_AUTO_BACKUP_ENABLED being deleted by the strip task means the new logic has no disable toggle. Implementation sequenced AFTER the strip lands (same backup.ts file — avoid conflict). Architect design dispatched in parallel (read-only).
- **2026-08-11 — OWNER BINDING DIRECTIVE #3 (env-var purge, refines ADR-0013 dispositions): "Strip [recoverTruncatedWal] and any env var that has no legitimate reason for existing other than protecting from an edge case. Don't just blindly add them to config without a real documented valid purpose. Auto backup should be enabled always, why would the store not repair, why would the store not validate? Periodic enrich — is this automatically triggered now? Heal stale vectors what even is that? Disable heal???"** Dispositions: (a) recoverTruncatedWal + SOX_ALLOW_AUTO_WAL_ASIDE — DELETE (ADR-0013 consequence; refusal-only stays); (b) SOX_AUTO_BACKUP_ENABLED — DELETE (backup ALWAYS on; keep SOX_AUTO_BACKUP_DIR as host config); (c) SOX_STORE_REPAIR 'off' — DELETE (store always repairs; detection always on); (d) SOX_STORE_VERIFY 'off' — DELETE (store always validates ≥ 'fast'; rigor selection stays); (e) SOX_DISABLE_PERIODIC_ENRICH — DELETE (periodic enrich is ALREADY automatic — in-process periodic tick per ADR-0007, no separate daemon; disabling it has no normal-purpose); (f) SOX_HEAL_STALE_VECTORS — DELETE the gate (the typed operator action memory_curate reheal_stale IS the interface; the gate currently also blocks the operator surface at memory-server:710 — wrong); (g) SOX_DISABLE_EMBED_HEAL — DELETE (dead on arrival: BL-344 proof — set on live unit, zero effect); (h) SOX_SYNC_EMBED, SOX_STORE_VERIFY_SKIP, SOX_MEMORY_LOG_DISABLE — EVALUATE with real documented purpose before any config conversion; no purpose → delete, not convert. ADR-0013 inventory dispositions to be updated to match actual decisions after the purge lands.
- **2026-08-11 — ADR-0013 LANDED + COMMITTED (5cd4c0a9): "Feature switches are typed config, never environment variables" (ACCEPTED).** D1 no new behavior-toggle SOX_* vars; D2 switches = typed config, default-explicit, reported in telemetry+health; D3 tuning constants may stay env-tunable but never gate a path; D4 one-shot operator actions = explicit CLI/typed calls never env-armed; D5 host-injected env = config, stays, deny-listed. Inventory (code-verified): violations to convert/delete — SOX_ALLOW_AUTO_WAL_ASIDE (DELETE on merge), SOX_SYNC_EMBED (convert or delete), SOX_HEAL_STALE_VECTORS (DELETE gate), SOX_DISABLE_EMBED_HEAL (convert), SOX_DISABLE_PERIODIC_ENRICH (convert), SOX_AUTO_BACKUP_ENABLED (convert), SOX_STORE_REPAIR (convert), SOX_STORE_VERIFY (drop 'off'), SOX_STORE_VERIFY_SKIP (border), SOX_MEMORY_LOG_DISABLE (border); border-selection keep — STORE_ADAPTER, SOX_EMBED_BACKEND, SOX_PROXY_BACKEND; tuning keep — SOX_WAL_SIDECAR_STALE_THRESHOLD_MS etc; host-config keep — SOX_CONFIG_*, SOX_PERM_*, TURSO_*, SOX_ECOSYSTEM_HOME, SOX_RUNTIME_FILE. Notable: SOX_CONFIG_HTTP_PORT does NOT exist (only SOX_CONFIG_PORT); SOX_HEAL_STALE_VECTORS revised from keep→DELETE-gate (gates even the typed operator surface); BL-344 incident cited as in-repo proof (var in .plist, absent from ps eww, false green).
- **2026-08-11 — TRIAGE LANDED (task ses_00d45e537ffemkTzEkX6iR40LC): NOTHING WAS LOST — NO RESTORE NEEDED.** Read-only forensic, byte-level WAL + row-level superset proofs on copies only (live never opened with writer). Incident WAL fully valid: 206,032 B = 32 + 50×(24+4096), leftover 0, magic 0x377f0682, ckptSeq 1500, salts f17b19b9/0f948e19, **50/50 frame checksums verify** (algorithm calibrated 12/12 against known-good crash-killed stock WAL first), 7 committed transactions (dbsz>0 frames 1,5,10,15,29,49,50), no dangling tail. Main DB at incident contained NONE of the WAL's frames (all 39 unique pages differed — fully unmerged). Row-level: main-only 10,747/55,336 → main+WAL 10,747/55,356 (WAL = exactly 20 RELATES_TO cluster-pass edges, t_created 19:31:09-17Z, 0 nodes) → current live 10,753/55,364. **0 nodes/edges exist only pre-recovery; all 20 WAL edges present in current DB.** Backups monotone: Aug-10 19:50:48Z 10,692/55,100 < Aug-9 10,646 < ... < Aug-2 9,513 — current and incident state both exceed every backup. THE RECOVERY ALREADY REPLAYED THE WAL: tshm moved aside 21:30:17Z, server pid 70323 opened, checkpointed at 21:32Z, WAL merged + truncated to 0 B, fresh tshm + openmark. Recurrence analysis: stale tshm salts differ per event (Aug-1 45e76573, Aug-3 ef0d80c8, Aug-4-era f17b19b9=current) ⇒ WAL epoch reset twice Aug 1→4, consistent with stock clean-close WAL-deletion on Aug 1/3 (D6 mechanism); Aug 11 was NOT a deletion (WAL intact, Aug-4 epoch) — MEDIUM confidence. Q2 writer: -shm at 19:31:29.366Z (3.75ms after WAL's last touch) is a STOCK wal-index (iVersion 3007000 LE @0, salts @32, tail page list matches 50 frames; Aug-1 stale -shm identical structure) — mechanism proven, process UNIDENTIFIED (checked+excluded: doctor-tick=registry-only, sox backup.ts=Turso VACUUM INTO never touches live -shm, shell history, crontab, launchd logs; remaining candidates: deployed bundle dist/index.js holds 10 better-sqlite3 refs — dropVec0/dropFtsResidue/integrity — adhd-side tool, or manual probe; frames are genuine server cluster-pass data regardless). Q3: backlog.db live 2,111/1,207 > pre-isolation-0808 2,079/1,196 > wal-full-0808 2,074/1,193 > Jul-30 594/273 > Jul-24 361/176 — no loss; Defect B schema-only. Q4 post-incident writes all survive (server poisoned 19:31:11Z, nothing wrote in the gap; post-recovery 6 nodes 10748-10753 + 8 edges checkpointed). Q5 recommendation: memory.db KEEP CURRENT AS-IS (restore would LOSE 61 nodes/264 edges); TAKE FRESH BACKUP NOW (auto-backup only runs on clean start — freshest is ~22h stale = operational gap); preserve forensic set (renamed tshm.stale-2026-08-11-recovery-*, stale sidecars, temp copies /var/folders/yg/cfczgtx54bzfh74lx2_mv0z80000gp/T/opencode/forensic-wal/) until owner accepts; WAL-replay path NOT NEEDED — already replayed, preserved incident WAL (206,032 B) valid + replayable if ever re-examined. backlog.db keep current. NEW FINDINGS (unfileable — graph FK-blocked + read-only mandate): (1) **no backup between Aug 10 19:50:48Z and recovery (~22h)** — auto-backup only on clean server start; recommend schedule-independent backups; (2) **incident report's '622→645 episodes' contradicts node-table ground truth (5,945/5,951 kind='episode')** — likely different metric from failed instance, needs reconciliation. Minor: stock integrity_check 'Page never used' on Turso stores = known cross-engine free-page accounting (server-side passes).
- **2026-08-11 — GATE + SMOKE LANDED (task ses_00d467562ffeuw0hSEbnRZGwJx, staging/gate-merge @ 2051375f): VERDICT GATE GREEN.** All three weave branches merge into main CLEANLY — zero conflicts, zero resolution required (debt-soxgraph-002 c29fa2b4 fast-forward 2 files; c-fts-object 3ef255cf 3-way 7 files; bl373-sidecar-staleness 56201baf 3-way 9 files). Each branch touches fully disjoint files. Smoke: **13 passed, 0 failed, 0 skipped** (`summary.json {"passed":13,"failed":0,"skipped":0}`); one pre-existing BL-173 noise (ownership.json mutated in shared ~/.adhd/sox-ecosystem — same on any smoke run, not a regression). Lint + typecheck: store-adapter ✅, graph-store ✅, memory-core ✅. Merge-order hazard: NONE (disjoint file sets; ping-health.ts new file with clean barrel export; bl373 memory-server index.ts change is bundle-member, not lib). Full suites: store-adapter 419/28 files, graph-store 118/10, memory-core 676+8skipped/70. Ready for main. NOTE: gate merged bl373 WITH the lossy recoverTruncatedWal still in the branch — architect Item 5 binding recommendation is to DELETE it before final main merge (refusal-only).
- **2026-08-11 — D6 EXTENSION LANDED (task ses_00d854300ffepLClHxbz6dEjsQ resumed, commit 56201baf on fix/bl373-sidecar-staleness, 8 files +602/−8; D6 44398c5c unchanged; tree-state CLEAN): BOTH OWNER REQUIREMENTS IMPLEMENTED.** **REQ A — PING HONESTY**: new pure helper `computePingHealthVerdict` (libs/memory-core/src/ping-health.ts:57) → {status:'ok'|'degraded'|'unhealthy', status_reason, store_ok, store_error}; status==='ok' ONLY when store opened AND embed real; store-failed-to-open ⇒ 'unhealthy' + store_ok:false + store_error (open failure verbatim); embed-degraded + store-healthy ⇒ 'degraded' (dimensions never collapse). Bundle wiring memory-server index.ts: storeBlock captures storeOpenError + storeOpened; poisoned-mid-session surfaces free (ping queries flow _ensureHealthy → D6-fixed _reconnect → heal or throw into catch ⇒ unhealthy); no extra write-probe (evaluated, rejected as redundant — existing queries exercise poisoned path). ok:true KEEPS RPC-success meaning across all 20 tools (documented choice; status is the health verdict). Docstring :323-326 updated. configured:false (BL-412) + absent-store ⇒ explicit store_error + unhealthy. Removed dead getClusterMetrics import (pre-existing typecheck error, previously masked by missing-dist resolution). **REQ B — ROOT CAUSE (mechanism CONFIRMED by scratch repro)**: crash-kill of Turso process ⇒ -tshm mtime freezes but WAL stays consistent, next Turso open SUCCEEDS — crash alone not the trigger. **A better-sqlite3 (stock-SQLite) writer on the same store: opens WAL mode, writes, closes ⇒ clean close CHECKPOINTS AND DELETES the -wal and -shm while the -tshm SURVIVES untouched (never maintained by stock engine). Next Turso open: frozen -tshm over moved/deleted WAL ⇒ I/O error short read — THE INCIDENT REPRODUCED FROM SCRATCH.** Forensic smoking gun: live memory.db-shm fresh at 19:31:29 while -tshm froze Aug 4 — Turso under multiprocess WAL NEVER creates -shm; only a stock-SQLite opener does ⇒ Aug-11 19:31:29 shm+wal touch was a MIXED-ENGINE WRITER on the live Turso store. ROOT: the -tshm is maintained ONLY while a Turso connection holds the store; any non-Turso writer moves the WAL without the sidecar. (iv) the 'lease' is the open connection; only multiprocess_wal flag activates -tshm maintenance; better-sqlite3 never sets it. **Proactive reconciliation** (proactivelyReconcileStaleSidecar, integrity.ts:736; wired in turso-adapter.ts connect BEFORE openOnce): mtime-proven stale -tshm moved aside before the driver tries — failed-open path never taken; staleness ref = WAL mtime, or DB mtime when WAL gone (mixed-engine deleted-WAL variant, caught by own test); -shm NEVER touched pre-open (self-reconciling; empty-WAL branch stays in catch — concurrent-reader safety under multiprocess WAL); catch remains backstop. Future-unhandled class covered: crash-kill, epoch change, concurrent opens (proactive never touches -shm; live connection WAL fresh ageDiff≈0), new driver version (fs-only heuristic engine-agnostic), stale sidecar + truncated WAL (proactive moves sidecar; recoverTruncatedWal Shape B opt-in; no opt-in ⇒ typed error + memory_ping unhealthy — closes loop with Req A). multiprocess WAL stays ON everywhere (ADR-0012, owner directive). **RED→GREEN (BL-225)**: ping verdict — pre-fix formula (04414dca index.ts:1136 embedState==='real'?'ok':'degraded') yields status:'ok' on incident inputs = false positive evaluated verbatim; new helper ⇒ unhealthy/store_ok:false. Proactive+root-mechanism tests — RED on 44398c5c (D6 catch-only: first open fails, 'damaged'/'blocked the open' event fires), GREEN on fix (no failed-open path, event stream proves catch never ran, all data Turso-seeded + better-sqlite3-written survives). Gates: store-adapter 417/417 + lint + typecheck; memory-core 672/8; memory-server bundle typecheck + typecheck-tests + lint clean (ungated targets; build/test memory-server untouched per deploy guard). Smoke deferred post-merge. Findings doc committed with mechanism + ping verdict shape. UNVERIFIED: exact process that was the Aug-11 19:31:29 stock-SQLite writer (better-sqlite3 opener vs sqlite3 CLI vs backup tooling) — mechanism proven by repro, culprit process needs logs. Process note: first pass at bundle edits landed in MAIN checkout — caught, verified diff entirely agent's, git restored main, redid in worktree; main clean. BL-373 recurrence backlog item STILL UNFILED (graph read-only); recorded in committed findings doc.
- **2026-08-11 — C-FIX REVIEWER LANDED (task ses_00d858eb5ffehYNYeYVeQ357XJ): VERDICT MERGE-READY (contingent on smoke-test gate).** Independent verification: lint/typecheck store-adapter+graph-store pass; nx test store-adapter 407/27, graph-store 118/10, memory-core 666/8 (matches implementer); independent real-engine repro (raw @tursodatabase/database@0.7.1 + better-sqlite3) reproduced all core empirical claims: FK-mismatch outage + 3-row materialization/DROP-rebuild semantics. Findings (sev-ranked): **F1 medium (merge gate): smoke test NOT run — mandatory pre-merge** (AGENTS.md unconditional for bundle-consumed libs/data; operator runs `rm -rf dist/smoke && node scripts/smoke-test.mjs` in isolated worktree pre-merge, record summary.failed===0). F2 low: absentTursoFtsMaterialization (fts-ops.ts:381-401) schema-read failure degrades to 'all absent' ⇒ DROP+CREATE of a HEALTHY index if the read errors — degrade-to-destructive direction wrong; fix: tri-state absent/present/uncertain, throw on uncertain. F3 low: adopt path (existing !== canonical, fts-ops.ts:244-250 vs 276-278) returns early, never verifies — half-materialized non-canonical index reported ensured:true, violating the fix's own invariant; fix: verify with adopted indexName on adopt path too. F4 info: name-presence-only verification (fts-ops.ts:416-458) — present-but-corrupted _key row passes; acceptable (matches orphan guard + measured panic trigger = absence) but 'healthy' claim stronger than check; optional: assert sql contains USING backing_btree. F5 info: DROP+CREATE not atomic w.r.t. concurrent opens — converges to healthy, not a panic state, no change required. F6 info: 'typed error' is plain Error with [BL-507] prefix, not error class. F7 info: spec RED-attribution imprecise (parent INSERT succeeds, child writeEdge fails) — discrimination unaffected. F8 info: hasExplicitRowidForeignKey misses compound FKs where rowid not first column (REFERENCES node(uid,rowid)) — Drizzle single-column is live shape, conservative direction acceptable; extend or document. Checklist call-outs: (a) DROP+recreate-once SAFE verified (DROP removes all 3 rows, re-CREATE rebuilds, fts_match works after, crash-between leaves fully-missing index which self-heals next open — strictly safer than half-materialized panic-bomb); (b) turso-gate FIRES when it should (regex matches backtick/double-quote/bracket quoting + spacing, no false positive on uid/implicit/id forms, sqlite arm pins byte-identical identity BL-448 AC-3; capability flag would be more durable but disproportionate for hotfix); (c) NO healthy-store regression (verify gated on !dialect.supportsShadowTable — false for SqliteFTS5Dialect fts-dialect.ts:149; one extra sqlite_master read on turso opens; edge rebuild preserves data via rebuildTable copy-then-swap BL-447/BL-448 machinery; one edge: legacy duplicate (src,dst,rel) rows would fail re-created ix_edge_unique mid-transaction at open — strict improvement over un-openable FK state, error won't mention BL-507); (d) test discrimination GENUINE (6 new tests name BL-507, turso arms executed 0 skips, RED arms structurally guaranteed). Git hygiene clean: 1 commit, 7 intended files +639/−1, no dist/registry/lockfile drift, hooks installed, tree-state attributable. Unverified ledger: live-copy acceptance (applySchema OK/writeNode rowid 2112/writeEdge OK/searchNodes 50 hits/edge DDL normalized) not reproducible without live store (mechanism corroborated by fresh-store turso arm + raw repro); smoke; GitNexus; explicit pre-fix red run (verified structurally + empirically, did not run pre-fix tree); live-data claims. Bottom line: both defects real + correctly fixed, tests genuinely discriminate, hygiene clean — run smoke at merge gate, republish both packages + relock consumers.
- **2026-08-11 — OWNER BINDING DIRECTIVES #2 (post-D6, both must be implemented): (A) PING HONESTY — `memory_ping` (and any health probe) must NOT report healthy when every write request is failing.** The debugger observed `memory_ping → ok:true, store:null` while the write path was dead — that is a health-check false positive and is itself a defect. Health must reflect the store's actual writability: a failed open, a null store, or a poisoned connection must flip the ping to unhealthy/error, never ok. (B) ROOT CAUSE, NOT DETECTION — "detecting a failed WAL is not the same as permanently resolving the root cause." The D6 mtime-heuristic + move-aside is a recovery mechanism; the root cause is that Turso's `-tshm` has NO SQLite-style self-reconciliation: the sidecar froze Aug-4 while the WAL continued (three recurrences, byte-identical sidecars, one mechanism). The implementation must (i) reconcile the stale sidecar PROACTIVELY at open (move before attempting open when mtime-proven stale), not only reactively in the catch after a failed open; (ii) investigate and fix the mechanism by which `-tshm` stops being maintained while the WAL continues (process-death/epoch-change under multiprocess WAL, launchd logs Aug 4-11, driver close/open semantics) rather than only healing the symptom; (iii) handle future unhandled scenarios in the right way — the fix must be structurally sound for the whole class, not just the three observed recurrences. Multiprocess WAL stays ON (directive #1, ADR).
- **2026-08-11 — OWNER BINDING DIRECTIVE (overrides architect D6 item 4): "WE ARE NOT TURNING OFF MULTI PROCESS WAL, THAT IS AN ADR — we need to make wal functionality correctly work."** multiprocessWal stays ON; it is an ADR decision. The permanent BL-373 solve must make WAL reconciliation work CORRECTLY under multiprocess WAL — mtime staleness heuristic, frame probe, sidecar-first recovery, `sidecar_stale` warning — NOT by flipping the default OFF. Architect D6 item 4 (flip default OFF for local-file stores) is RESCINDED; no consumer audit needed to justify keeping it ON; do not propose flag-flips in any future work order. Validated by C-fix evidence: multiprocessWal was REFUTED as a cause of Defect A (identical 3/3 materialization with flag on/off) — keeping it ON costs nothing for the incident fixes.
- **2026-08-11 — C-FIX LANDED (task ses_00dae5aceffeb3O71XhoniGLXH, branch fix/c-fts-object @ 3ef255cf, 7 files +639/−1, UNMERGED, ready for review).** Reproduction (temp dirs only, live stores untouched): malformed object present on EVERY fresh store (sqlite_master row 22 `__turso_internal_fts_dir_idx_fts_node_key`, first row stock-SQLite cannot parse; emitted by `TursoFTSDialect.createIndexDDL` via graph-store.applyFtsSchema → ensureFtsIndexOn) BUT writes work through turso on fresh stores (writeNode/writeEdge/searchNodes OK, both codebases). FK mismatch reproduced on a COPY of live backlog.db (carries both FTS stacks: Drizzle-era rows + fts5 residue rows 21-28 + Tantivy rows 31-33). **TWO INDEPENDENT DEFECTS (root cause, file:line): Defect A — the 'malformed object' is HEALTHY Tantivy backing, not corruption; created by graph-store.applyFtsSchema (index.ts:1062-1068) → ensureFtsIndexOn (fts-ops.ts:225-290) → createIndexDDL (fts-dialect.ts:252-267) → TursoAdapterImpl.exec (turso-adapter.ts:709-718, verbatim forward, NO silent no-op on CREATE); breaks only stock-SQLite tooling (BL-329 typed E-TursoNativeStore); REAL code gap = ensureFtsIndex returned ensured:true WITHOUT verifying 3 rows landed (orphan guard caveat fts-orphan-guard.ts:264-284: Turso can report DDL success it did not perform) → half-materialized index = BL-361 panic-bomb. Defect B — the production outage: live store Drizzle-era edge DDL `src INTEGER NOT NULL REFERENCES node(rowid)` explicit-rowid FK; with PRAGMA foreign_keys=ON (graph-store index.ts:17-22) turso/libSQL CANNOT resolve explicit rowid alias → every statement dies `foreign key mismatch referencing "node"`; bisection fresh stores foreign_keys=ON: `REFERENCES node(rowid)` FAILS, implicit `REFERENCES node` OK, `REFERENCES node(uid)` OK; stock SQLite resolves all three; malformed object is NOT the FK cause (Defect B fires with FTS rows deleted entirely). Attribution: (a) graph-store emits Tantivy path for turso — CONFIRMED; (b) exec() silent no-op / torn write — REFUTED (materialization complete; BUG-TURSO-ADAPTER-EXEC-DDL-SILENT-NOOP-001 concerns DROPs of fts5/vec0, unrelated); (c) fts5 trigger bodies never fire — true but irrelevant (turso branch never installs them; live store's are sqlite-era residue); (d) multiprocessWal — REFUTED (identical 3/3 on/off). **FIX: fts-ops.ts:264-277 turso branch verifies 3-row materialization after create; fts-ops.ts:416-458 verifyTursoFtsMaterialization DROP+recreate once (DROP removes all 3 rows, CREATE rebuilds), typed error if still incomplete — never ensured:true on a panic-bomb; graph-store index.ts:1012 hasExplicitRowidForeignKey detection; index.ts:1137-1148 ensureCheckConstraints rebuilds edge via existing rebuildTable machinery with EDGE_TABLE_DDL (implicit FK) GATED ON TURSO (sqlite keeps byte-identical identity, BL-448 AC-3); tools/eslint-local/no-storage-backend-leak.cjs:155-168 cited exception for turso gate. Preserved: memory-core delegation, sqlite FTS5 byte-identical, A2, AC-6, BL-461 adoption guard.** Tests red→green (BL-225, real engines, no mocks): store-adapter 407 passed (27 files, +2 new), graph-store 118 passed (+4 new), memory-core 666 passed 8 skipped; lint/typecheck green; no failing test names. DEFINITIVE ACCEPTANCE: fixed dists applied to copy of LIVE backlog.db → applySchema OK, writeNode OK (rowid 2112 into live copy), writeEdge OK, searchNodes OK (50 hits), edge DDL normalized:true — live store will self-heal at next open once fix ships. Release path: BOTH packages — republish @adhd/sox-store-adapter 0.5.1→patch + @adhd/sox-graph-store 0.8.1→patch; adhd reinstall/relock both (operator D step restores backlog CLI). BL-507 NOT filed (backlog tool down with the very FK error it fixes — outage self-evidenced; full item body preserved at .opencode/artifacts/reports/BL507-c-fix-fts-report.json). Smoke test deferred by deploy guard (builds memory-server); run post-merge. GitNexus CLI unusable (repo disambiguation defect — tooling gap filed in report). Vector-store 3 pre-existing no-storage-backend-leak warnings (BL-380 family, pre-existing, non-blocking).
- **2026-08-11 — ARCHITECT LANDED #2 — BL-373-FAMILY PERMANENT SOLVE (task ses_00d99dcadffe1bMEfaIEC2qqPK resumed): binding work order for C-fix.** Key structural finding: `_reconnect()` (turso-adapter.ts:240-254) replays full `connect()` via captured `_connectOpts` (:574-577) → fixing `recoverStaleWalIndex` heals BOTH fresh-open AND poisoned-cached-connection paths in one place. Error-math finding: offset 61832 = 32 + 15×4120 = WAL ends EXACTLY at a frame boundary → short-read consistent with stale sidecar over a CLEAN frame-aligned WAL (Shape A), not necessarily mid-frame truncation (Shape B); probe must not conflate. **1. STALENESS HEURISTIC**: mtime-based sidecar-vs-WAL with fixed threshold default 60 000 ms (env `SOX_WAL_SIDECAR_STALE_THRESHOLD_MS`), `statSync().mtimeMs`; stale ⇔ `walMtimeMs - tshmMtimeMs > THRESHOLD` → move `-tshm` aside only (NEVER `-shm` on this path — SQLite self-reconciles its shm, turso -tshm has no self-heal; moving shm under concurrent reader = new corruption risk); ambiguous (0..THRESHOLD or negative) → frame probe, decline with evidence-carrying message (replace 'may legitimately describe it' integrity.ts:336-338 with mtimes+diff+threshold+probe). Threshold fixed not proportional (wall-clock epoch question). **2. WAL TRUNCATION**: sidecar-first → reopen against existing WAL (Shape A done, no WAL touched); frame probe `probeWalFrames(walPath)` deterministic fs-only: magic 0x377f0682/0x377f0683, page size u32@8 power-of-two 512-65536, `leftover = (size−32) mod (24+pageSize)`; truncated ⇔ leftover≠0 (Shape B); WAL-aside `${dbPath}-wal.corrupt-<stamp>` LAST RESORT default OFF — without `SOX_ALLOW_AUTO_WAL_ASIDE=1`, emit typed operator-action error (store already unopenable, nothing lost by waiting); auto-move gated on BOTH probe-truncation AND mtime-stale; truncated WAL with fresh sidecar = genuinely ambiguous → decline to operator. **3. SELF-HEAL+OBSERVABILITY**: active recovery stays turso-adapter.ts:441-468 (reconnect coverage is free); add `warnIfStaleSidecar(dbPath)` in integrity.ts called from connect() near preflight marker (turso-adapter.ts:418-427), emit `emitIntegrityReport(dbPath,'sidecar_stale',...)` with mtimes+diff+threshold BEFORE open AND when open succeeds (masked case); extend IntegrityReportEvent union (near integrity.ts:2130-2140 — exact line unverified); recovery detail string gains mtime evidence. **4. multiprocessWal — RESCINDED BY OWNER (see directive above); no flag flip; validate reconciliation under multiprocess WAL ON.** **5. ATTRIBUTION**: read-only forensic procedure for C-fix (no driver, no SQL): stat all artifacts mtimeMs; read first 32 bytes of memory.db-wal (magic/version/page/ckpt-seq/salts/checksums); run frame probe on live file (predict leftover 0); capture -tshm + preserved stale sidecars; correlate ckpt-seq + -shm + -openmark to date WAL epoch; candidates vs timestamps: VACUUM INTO path (turso-adapter.ts:601, incompatible with multiprocess WAL per :596), checkpoint activity, Aug-4 deploy/restart. Deterministic fixtures honoring BL-361 (present never absent): real turso store + `fs.truncateSync(walPath, 32+K×4120+100)` = Shape B; truncate to exactly 32+K×4120 = clean-boundary case that must NOT classify as corruption. **6. TESTS** red→green BL-225 naming BL-IDs: (a) backdated sidecar 7d ⇒ attempted:true movedAside length 1, assert -shm untouched, connect succeeds data survives; (b) 30s<60s ⇒ declined untouched, 90s ⇒ moved; (c) Shape B mid-frame ⇒ probe truncated:true, with SOX_ALLOW_AUTO_WAL_ASIDE=1 ⇒ WAL renamed .corrupt-<stamp> preserved + reopen from ckpt, without ⇒ declined typed error; clean-boundary ⇒ NOT corruption (sidecar moves WAL stays); (d) startup warning 'sidecar_stale' captured incl. masked success case. Existing test integrity-selfheal.test.ts:1096-1107 UPDATED DELIBERATELY (fixture fresh sidecar fresh WAL ⇒ still declines within threshold; move assertion off `/holds 4096 bytes/` to evidence message; rename to 'declined when the sidecar is not provably stale'). **7. RELEASE**: ride planned 0.5.2 incident patch (one combined release, NOT separate 0.5.3); internal order sidecar-reconciliation FIRST (unblocks live server open class), malformed-object reorder SECOND (unblocks backlog D); fix reaches live server only via owner-gated rebuild+deploy regardless (bundle 6eff093e3991 predates everything). **48H ORDER**: 1) operator hand-recovery now (move memory.db-tshm aside); 2) C-fix items 1-3+6 code+tests; 3) C-fix forensic attribution (item 5, read-only); 4) publish 0.5.2+0.8.2, adhd relock; 5) owner-gated rebuild+deploy; 6) multiprocessWal decision — RESCINDED, stays ON.
- **2026-08-11 — MEMORY READ-PATH DEBUGGER LANDED (task ses_00dbadb41ffeC5mDPY2NTo5UKT): RESOLVED (diagnosis only, no files touched).** Root cause: live memory server read path is DOWN because `~/.memory/memory.db-tshm` is 7 days stale (mtime Aug 4 14:16) while `memory.db-wal` holds 206,032 bytes and is non-empty/growing → EVERY fresh open hits Turso 'short read on WAL frame at offset X' (BL-373 trap); `recoverStaleWalIndex()` correctly DECLINES to move sidecar because WAL is non-empty (dist/index.js:60102-60105 → 62022-62023) so open throws. Long-lived backend pid 91094 served reads from cached adapter until 19:31:11Z when WAL reset poisoned it (`reset failed: I/O error: short read on WAL frame at offset 4152`); every reconnect since fails identically. Telemetry: 19:31:11Z store_adapter.turso.connection.poisoned; enrich ticks collapsed 17-19s → 1-9ms after 19:31; enrich tick 261 at 19:24:26Z showed 100,598ms blocked_ms (event-loop starvation right when fresh-opens first failed). **CRITICAL REFINEMENTS:** (1) the original 0-recall claim (15:55Z) is NOT reproducible on pid 91094 (window clean; recall returned vec+fts+temporal hits at 19:21-19:24Z); reporter's pid 88179 died Aug 9 → recall-0 observations predate the current failure; embeds_completed=6/coverage 12.6% ≠ server counters now (30/0.694) — likely from the dead instance. (2) memory_list_entities -32001 is a SEPARATE ALGORITHMIC defect: 4,238 entity nodes × ~23.3ms per-entity getEdges (list-entities.ts:74-106, N+1) ≈ 98.7s > 60s MCP timeout (reproduced twice at exactly 60,015/60,020 ms, no enrich running). (3) the 3.4-billion-ms awaiting timestamps are REAL episode ages (2026-06-21..2026-08-11), NOT corrupt. (4) **BACKLOG WRITE BLOCKER CHARACTERIZED: the explicit `REFERENCES node(rowid)` FK form fails on Turso with foreign_keys=ON for ANY table — reproduced on scratch tables in BOTH stores; implicit `REFERENCES node` works; graph-store PRAGMAS force foreign_keys=ON. The malformed `__turso_internal_fts_dir` object is NOT the write blocker (memory store carries it and writes fine); backlog.db ALSO has fts5 residue (fts_node VT + 3 triggers + shadow tables) distinct from memory store — dropping it did not fix the FK error.** (5) deployed bundle confirmed: dist shasum 6eff093e3991 == registry == running, built from tree ~dff020e0 (2026-08-08 19:46), PREDATES d5296b3b (FTS delegation) and 86738ef5 (BL-461) — running recall is old hand-rolled FTS arm. (6) foreign_key_check clean on both copies; integrity_check only known BL-360 false positive. FIX NOT APPLIED — operator decision required: only in-band recovery = move ~/.memory/memory.db-tshm aside by hand (BL-373 documented escape, declined automatically because WAL non-empty) after clean WAL checkpoint, OR full restart of launchd backend with sidecar moved. Open questions: what truncated/replaced WAL on Aug 4 (unverified); repair vehicle for backlog FK is architect task D5 (rewrite edge FK to implicit form or drop/recreate edge table — NOT touch FTS object). NEW DEFECTS TO FILE (backlog graph read-only, so logged here pending writable graph): (a) BL-373-family recurrence — stale -tshm vs non-empty WAL is a PERMANENT outage, not self-healing; (b) memory_list_entities N+1 → guaranteed -32001, needs single-query join rewrite; (c) reporter evidence caveat — original recall-0 claim not attributable to current backend, re-verify against pid 91094 before filing as new defect.
- **2026-08-11 — ARCHITECT LANDED (task ses_00d99dcadffe1bMEfaIEC2qqPK): binding decision record, READ-ONLY, all file:line claims verified against branch c29fa2b4 + main 04414dca.** Path corrections: memory-core = `libs/memory-core/src/db.ts`; fts-ops.ts/fts-dialect.ts/graph-store index.ts identical between main+branch for A2 machinery; graph-store untouched by DEBT-002. **D1 MERGE DEBT-002 NOW conditionally**: source merge ≠ live deploy (live bundle 6eff093e3991 runs old hand-rolled block); branch orthogonal-to-mitigating, pre-check prevents hybrid state; deploy hard-gated on smoke-test green + store-adapter 0.5.2/graph-store 0.8.2 published+relocked OR owner sign-off. **D2 incident-fix design**: (a) reorder fts-ops.ts turso branch — refuse-to-create atop fts5 residue (extend FtsEnsureResult with `residueNames`, set residueNeedsOutOfBand=true, ensured=false, return) — NEVER create Tantivy index atop residue; DROP of fts5/vec0 through turso silently no-ops (verified fts-dialect.ts:50-54), real drop mechanism = better-sqlite3 `unsafeMode(true)` + `PRAGMA writable_schema=ON` (preflight.ts:50-68, 196-200, 265-276) so helper heals even hybrid files (delete Tantivy rows first, then fts5 residue); (b) graph-store applySchema/applyFtsSchema/reapplyFtsInTx MANDATORY pre-check via promoted shared store-adapter helper (e.g. `ensureNoFts5ResidueOnTurso(adapter, dbPath)`), reassign this.adapter from reopened result; (c) do NOT change exec() contract — add verify-by-contract helpers (verify drop absence in sqlite_master; verify create materialized 3 rows per preflight.ts:72-80, absent ⇒ ensured:false + typed diagnostic = direct fix for BUG-TURSO-ADAPTER-EXEC-DDL-SILENT-NOOP-001); (d) exact targets: fts-ops.ts :231-290 reorder+verify-create+residueNames, :316-321 dead-code-delete; fts-dialect.ts no change; new shared helper in store-adapter (preflight.ts or new residue.ts); graph-store index.ts :1024-1051 applySchema, :1062-1068 applyFtsSchema, :1072-1095 reapplyFtsInTx; memory-core db.ts (branch) replace local pre-check with shared helper. **D3 PATCH RELEASE forced**: store-adapter 0.5.2 + graph-store 0.8.2 carrying D2+D4 fixes; backlog read-only blocks all filing/resolution repo-wide (verified create-item fails at SqliteGraphBackend.writeNode); publish-verify mandatory (0.5.0 lesson); do NOT fold multiprocessWal default change (turso-adapter.ts:376-378,506) into incident patch — own decision by BL-373 owner. **D4 telemetry**: attach failing SQL to error at throw site in fts-ops.ts create loop (:256-259) `(err as {sql?:string}).sql = stmt`; branch db.ts:636-640 logs `sql: truncateForLog((err as {sql?:string}).sql ?? '<unknown>')`; do NOT extend FtsEnsureResult (degrade path throws); fold into DEBT-002 pre-merge OR C-fix patch — recommend pre-merge. **D5 TWO-DEFECT model, in-place heal through fixed code**: Defect A malformed-object class (sox-side, D2 fix); Defect B FK-form `REFERENCES node(rowid)` (attribution UNVERIFIED: adhd-Drizzle generator vs legacy sox-V1 — C-fix must read live backlog.db sqlite_master read-only to attribute; sox current DDL = bare REFERENCES node index.ts:99-102/299-302/361-364; (rowid) form survives 0.8.1 open path because open-schema migration rebuilds edge on CHECK drift not FK column-list drift ⇒ sox-side: extend open-schema migration / ensureCheckConstraints-style repair to normalize legacy (rowid) FK (BL-313 skipDrop-interleaved rebuild, open-schema-migration.ts:15-16), verified by fresh create-item; adhd-side migration ONLY if inspection proves Drizzle-generated — that's adhd's migration not sox's); better-sqlite3 paths: raw open CANNOT open Tantivy-carrying file (errors.ts:13-36), SqliteAdapterImpl refuses typed E-TursoNativeStore (errors.ts:43-63), preflight openSchemaReader CAN (writable_schema, preflight.ts:196-200); both live DBs opened via turso default (factory.ts:24) so turso app path fine, better-sqlite3 tooling needs repair path (extend SqliteAdapterImpl open probe errors.ts:33-36 with opt-in writable_schema drop of __turso_internal_fts_dir_* + idx_fts_node rows, never silent on routine open); restore-from-backup = owner-gated fallback only (data-loss tradeoff explicit). **EXECUTION ORDER 48h**: 1) merge DEBT-002 today (smoke test first, fold D4 telemetry, pathspec commit); 2) dispatch C-fix work order per D2 (a→e: reorder, shared helper + rewiring, graph-store pre-checks, verify-create, red→green tests naming BL-IDs); 3) C-fix inspects live DBs read-only (attribute edge DDL, confirm hybrid vs clean-turso); 4) publish 0.5.2+0.8.2, publish-verify, adhd relock, consumer smoke; 5) D backlog recovery (fresh create-item green, fixed open path heals A+B); 6) memory.db recovery (turso app path; stale -tshm = BL-373 owner decision, recoverStaleWalIndex machinery exists turso-adapter.ts:441-468; multiprocessWal rides BL-373); 7) 48h parallel lanes (recall-0 debugger in flight, embed stall, BL-373 owner decision).
- **2026-08-11 — C-fix OBSERVED MID-FLIGHT (worktree check)**: `fix/c-fts-object` now has UNCOMMITTED changes in `libs/data/store/store-adapter/src/fts-ops.ts` + `libs/data/graph/graph-store/src/index.ts` + 3 new BL-507 specs (`fresh-store-backlog-open-path.bl507.spec.ts`, `open-explicit-rowid-fk.bl507.spec.ts`, `fts-ensure-turso-backing.bl507.spec.ts`) — agent is actively implementing exactly the D2 design targets (reorder fts-ops, graph-store applySchema pre-check, verify-create). No commits landed yet; debugger still not landed (no new artifacts).
- **2026-08-11 — DEBT-SOXGRAPH-002 reviewer LANDED (task ses_00db2811affefe0KDixtcRtbgD): VERDICT MERGE-READY YES-WITH-CAVEATS.** Checklist: delegation call shape correct (db.ts:593-598, weights exact, sqliteDDL as data); capability-gated pre-check correct AND necessary (db.ts:562-589 — turso branch only *reports* residueNeedsOutOfBand, never drops fts5 residue in-band; without pre-check the hybrid state fts5-residue+tantivy-index permanently blinds better-sqlite3); degrade-on-failure preserved (db.ts:626-649) with 1 low drift (failing SQL no longer logged — old code logged `sql:`); fresh-store drift only backfill-on-empty (idempotent reopen verified); AC-6 rg PASS (zero FTS SQL); tests all green (memory-core 670/0, store-adapter 405/405, graph-store 114/114, lint/typecheck ✔, tree-state CLEAN); red→green discrimination PROVEN BY CODE (old db.ts has 0 INSERT INTO fts_node → backfill-on-reopen red; empirical probe run INCOMPLETE — disposable worktree lacked opentelemetry deps); git hygiene CLEAN (1 commit, 2 files). **INCIDENT ANGLE (critical): branch is ORTHOGONAL-TO-MITIGATING; malformed-object class is UPSTREAM in store-adapter fts-ops.ts create-then-report ordering (fts-ops.ts:252-260 create → :316-321 report-only residue) + graph-store applyFtsSchema (no ordering pre-check). `__turso_internal_fts_dir_idx_fts_node_key` is Turso-engine provisioning (Tantivy segment store), not sox-named. This branch's pre-check is the reference ordering the incident fix should adopt. Do NOT hold this branch for the incident.** Caveats: (1) telemetry regression low — restore `sql` context to FTS-degrade log.error before merge if incident triage depends on it; (2) probe worktree `.worktrees/debt-002-probe` created by reviewer — REMOVED by dispatcher 2026-08-11 (only untracked node_modules, no source changes); (3) pre-merge gate: run `node scripts/smoke-test.mjs` before merge (touches memory-core open path consumed by bundles); (4) backlog resolve-blocker confirmed EXTERNAL (FK error), item left OPEN with no partial mutation.
- **2026-08-11 — probe worktree cleanup executed** (`git worktree remove --force .worktrees/debt-002-probe`, exit=0; verified gone from worktree list). Reviewed worktree `debt-soxgraph-002` untouched (still c29fa2b4).

## Key facts

- turso 0.7.1/0.7.2 rejects explicit-rowid FK + WITH RECURSIVE; VACUUM unavailable (experimental); user_version persists across reopen/rebuild
- multiprocessWal default ON in published 0.5.1 despite misleading FIXED marker on BUG-STOREADAPTER-MULTIPROCESS-WAL-DEFAULT-ON-001
- 12 zombie backlog serve processes killed Aug 8 (none running now)
- backlog create-item FK error blocks ALL backlog filing/resolution repo-wide
