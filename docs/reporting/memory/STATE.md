# Memory Restoration — Resumable State

> Single source of truth for **where we are**. Anyone (or any agent) picking this up cold should read
> only this file plus the linked ones.
>
> **Entry point:** [`README.md`](./README.md) — read that first if you are new here.
>
> Companion docs: [`PLAN.md`](./PLAN.md) (build order + per-packet ledger) · [`sandbox/README.md`](./sandbox/README.md) (sandbox spec)
> · [`../../observability/README.md`](../../observability/README.md) (how to read the logs) · `BACKLOG.md` (all items)

**Last updated:** 2026-08-03 19:22Z · **Branch:** `wip/turso-live-metrics`

> **What is measured vs. what is asserted.** Every number in the "Live service" and "Progress"
> sections below was read from the running server or derived from `BACKLOG.md` at the timestamp
> above. Everything under "History" is a dated record of a past event and is not re-verified — do not
> quote it as current. This split exists because the previous revision of this file carried a
> prominent, confidently-worded warning that had been false for two days.

---

<!-- PLAN-STATUS:BEGIN -->
<!-- Generated by tools/plan-status.mjs from BACKLOG.md. Do not hand-edit — run the tool. -->

## Progress — derived, not asserted

Regenerate with `node tools/plan-status.mjs` (`--check` in a guard). The per-packet ledger lives
in [`PLAN.md`](./PLAN.md); this is the summary.

| | |
|---|---|
| Packets complete | **14 / 56** |
| Packets partial | 3 |
| Packets not started | 39 |
| Open backlog items, in scope | **52** |
| Open backlog items, out of scope | 29 |
| In-scope items with no packet | 4 (BL-414, BL-415, BL-416, BL-424) |

**Done:** PKT-01 PKT-04 PKT-05 PKT-06 PKT-08 PKT-10 PKT-14 PKT-15 PKT-17 PKT-36 PKT-46 PKT-49 PKT-50 PKT-52

**Partial:** PKT-03 PKT-28 PKT-37

<!-- PLAN-STATUS:END -->

---

## The goal, unchanged

Restore the live memory-server to full functionality on **Turso**, without reverting to sqlite and
without giving up multi-process writer concurrency. Production-grade means no manual recovery step
and no crash path that can lose data.

---

## Live service — measured 2026-08-03T19:22Z

Server: pid **85177**, artifact **`a4892123287b`**, up since 2026-08-02T00:36Z. Store
`~/.memory/memory.db`, adapter **turso**, 4947 episodes / 9657 nodes.

| Subsystem | State | Evidence |
|---|---|---|
| Keyword search (FTS) | ✅ working | `fts_index_live` probe: all 3 sentinel rows round-trip through `idx_fts_node` |
| Vector recall | ✅ working | `embed_backlog: 0`; 4947/4947 episodes stamped, `unstamped: 0`, `stale_vector_count: 0` |
| Embedding | ✅ real inference | `bge-base-en-v1.5`, execution provider **coreml**, `embeds_completed: 126`, **`embeds_failed: 0`**, `applies_gone: 0` |
| Write path | ✅ healthy | write queue depth 0, in-flight 0, no busy/deadline rejections; `time_to_vector_ms` p50 1684 |
| Integrity | ✅ `overall: ok` | deep probes, 615 ms, `damaged: []`, all 5 probes `validated: true` |
| Backup | ✅ working | BL-385 resolved 2026-08-01 |
| Entity search | ✅ working | BL-384 resolved 2026-08-01 |
| Near-duplicate detection | ✅ live KNN | BL-381/BL-386 resolved; ⚠️ manually-merged pairs still report a fabricated `cosine_sim: 1.0` (BL-398) |
| Telemetry | ⚠️ initialised, unused | `role: "live-service"` (BL-404's fix is live) but **`stages_declared: 0`** — nothing declares spans, so BL-401 is unmet in production, not just in the repo |
| **Enrichment** | ❌ **DEAD — 22.5h** | `state: "stalled"`, `queue_depth: 46`, `queue_last_done_at: 2026-08-02T20:52:22Z`, watermark still `"pass":"legacy"` from 07-31. **BL-413**, filed 2026-08-03 |
| **Clustering** | ❌ **inert** | `cluster_count: 139` but `total_clustered: 0`, `coverage: 0`, `with_community: 0` against 4947 episodes. The `cluster.ts` incremental path is still a `return { clusters: [] }` stub. **BL-326 / BL-349** |
| WAL checkpoint | ❌ never runs | `last_checkpoint_at: null`, `wal_bytes: 1,470,872`. **BL-405** — the SIGTERM race is fixed, the checkpoint is not |

**Residual data defects on the live store:** 1 malformed row (rowid 9284, column `tags` — BL-342
residual), `stamped_without_vector: 1`, and 2214 of 4947 episodes with no topic (a consequence of
BL-413, not an independent defect).

**Loudest thing in production:** 154 occurrences/day of swallowed
`store.error: "prepare failed: Parse error: no such column: meta"` — 14% of all events emitted
2026-08-03, on a store reporting `overall: ok`. **BL-399**.

---

## What to do next

1. **PKT-54 (BL-413)** — the enrichment pass is dead. Do this **before** any clustering work:
   clustering consumes what enrichment produces, so fixing the stub first yields a working component
   with an empty input. Rule out a silently-dropped `SOX_DISABLE_PERIODIC_ENRICH` (BL-375/BL-378)
   before anything else — it is the cheapest possible cause.
2. **PKT-29 (BL-349 + BL-326)** — implement the clustering strategy. The spec has been finished and
   sitting unimplemented at [`findings/pkt28-clustering-strategy.md`](./findings/pkt28-clustering-strategy.md)
   since 2026-07-31; **PKT-29 was never dispatched.** This is the largest single functional gap.
3. **PKT-53 (BL-412) → PKT-48 (BL-405)** — stop the test suite opening the live store, then
   re-measure the checkpoint on a quiescent machine. BL-412 is plausibly BL-405's cause, not its
   sibling: under `multiprocess_wal` a checkpoint cannot truncate while another process holds a
   reader slot.
4. **PKT-41 (BL-391) + PKT-19 (BL-329)** — the Turso FTS/read-only pair, which gate recall quality.

---

## Known-unverified — do not treat as done

- **BL-412's fix is committed (`91cdd35`) but its red→green was never run.** The worktree had no
  `node_modules`, the agent used `--no-verify`, and no one has since executed the test. Under BL-225
  that is not a fix, it is a claim.
- **BL-405 is half-verified.** The pass condition was the absence of a reaper escalation line on
  restart, and it is absent — the two racing SIGTERM listeners are genuinely gone. But the WAL grew
  (3,563,832 → 3,596,792 bytes) across a clean restart instead of truncating, and
  `last_checkpoint_at` is still `null` two days later.
- **BL-409's mitigation covers half the problem.** Pathspec commits stop you sweeping another
  agent's *staged* files; they do nothing about their *uncommitted edits to the same file*, because
  the working tree is shared. For hot files (`BACKLOG.md`, `PLAN.md`, this file) it buys nothing.
  The structural fix is per-agent worktree isolation, which is now the default for dispatch.

### ⚠️ Worktree dispatch has a gap that has already cost verification

Worktrees created by `isolation: "worktree"` inherit **neither `node_modules` nor git hooks**. Two
agents (PKT-31, PKT-53) could not run `nx test` at all and committed with `--no-verify`. Before
dispatching into a worktree, either run `pnpm install` and `node tools/install-git-hooks.mjs` in it,
or accept that the packet's acceptance test will not run — and say so in the report rather than
letting `--no-verify` pass as green.

---

## Traps that cost real time (don't rediscover these)

- **`grep` is a shell function** and silently returns nothing on a file with a raw NUL byte. Use
  `/usr/bin/grep` when proving something is *absent*. (BL-371.)
- **A stale `-tshm`** makes the store unopenable with an error naming the *wrong* file
  (`short read on WAL frame` while the WAL is 0 bytes). The adapter now handles this at open —
  renaming, never deleting, and only when the WAL is empty. (BL-373.)
- **A sentinel token must be a WHOLE word.** The FTS probe capped tokens at 20 letters, so any row
  containing e.g. `sharedFastembedProcess` reported unindexed on a healthy index — 7.3% of live
  rows. Never truncate a term you are about to search for. (BL-374.)
- **Turso experimental flags are an ARRAY, not an object** — `experimental: ['index_method', 'multiprocess_wal']`.
  `index_method` is required on any connection that will run `fts_match`.
- **A read-only Turso connection cannot run `fts_match`** (`Resource is read-only`) even though
  plain reads work. This is BL-391 and it is why federated recall's BM25 arm is dead.
- **`PRAGMA integrity_check` has a permanent false positive** on any Turso store with an FTS index;
  the probe now filters exactly one known instance. Do not "fix" the store because of it. (BL-360.)
- **Turso PANICS and aborts the process** when opening a store whose FTS index row has no backing
  directory table — so a damage fixture must produce empty-but-present, never absent. (BL-361.)
- **`nx build` deletes the artifact BEFORE knowing the rebuild succeeds** (BL-235), and `--dry-run`
  is silently ignored on a run-target. A diagnostic build is a destructive operation; it has taken
  this server down twice.
- **`registry:sync-index` rebuilds transitively.** Running it after touching an unrelated package
  re-bundled `memory-server` and armed a production deploy nobody chose. (BL-390, now guarded by a
  dirty-tree refusal.)
- **`soxe service enable` rebuilds unit env from YOUR SHELL** and silently drops anything absent. It
  dropped both emergency brakes while reporting success. Diff the plist afterwards. (BL-375.)
- **`ProcessType` must not be "improved" to `Adaptive`** — Adaptive promotes out of Background on
  XPC activity, which sox services never generate. It would look like a fix and change nothing.
  (BL-331.)
- **`duration_ms` is wall-clock** and accrues during system sleep — every p90/p99/max from telemetry
  is inflated. (BL-369.)
- **BL ids collide.** Allocate with `tools/allocate-bl-id.mjs`, never by eye — and delete the
  `RESERVED` placeholder it writes before committing, or the marker check fails on a duplicate id.
- **Never enqueue onto a WriteQueue from inside a task already running on that same queue** — this
  deadlocked `memory_write` on any content over 2000 chars. (BL-154.) ⚠️ **This is a re-entrancy
  rule only.** It is *not* a mandate to serialise Turso writes; do not flip the Turso adapter's
  `needsWriteSerialization` / `concurrentTransactions`. Three agents have made that mistake.

## ⚠ Do not gate on a memory-core failure COUNT

Three runs of the **same** configuration returned **109, 95, 91** failures. The suites flake under
concurrent DB access, so a count difference between two runs is noise. **Diff failing test NAMES
instead** (`comm -13` on sorted lists). Now tracked as BL-202 with a packet (PKT-55) — until that
lands, no count-based claim about these suites is admissible.

---

## Rollback points

| Path | Contents |
|---|---|
| `~/.adhd/sox-ecosystem/memory/prerebuild-20260731-150126/` | db + WAL, **working `dist/`**, store-adapter + memory-core dists, plist |
| `~/.adhd/sox-ecosystem/memory/prerestart-20260731-174637/` | db + WAL, stale `-shm` / `-tshm` / empty `-wal` |
| `~/.memory/memory.db.20260731-132556.pre-fts-rebuild` | pre-repair store + WAL |
| `~/.memory/memory.db.20260730-123926.golive-verified` | last known-good go-live snapshot |

The `dist/` copy matters: `nx build` deletes the artifact **before** knowing the rebuild succeeds.

---

## Deploy procedure

**Canonical: [`docs/spec/service-lifecycle.md` §9.4a](../../spec/service-lifecycle.md) —
`[inv:deploy-verified]`.** Do not restate it here or anywhere else; that is the one copy.

Two things it exists to stop, both of which have fired:
- `launchctl kickstart -k` restarts the **proxy only**; the backend survives as a `PPID 1` orphan
  still serving the old bundle while every check reads green. The **artifact hash** from
  `memory_ping` is the only proof. (BL-372.)
- `soxe service enable` rebuilds the unit env from your shell and silently drops what it does not
  find. (BL-375.)

---

## Guards to run before any commit

```
node tools/check-backlog-markers.mjs     # derives open count from heading markers
node tools/plan-status.mjs --check       # fails if PLAN.md/STATE.md status has drifted from BACKLOG.md
node tools/check-no-nul-bytes.mjs
/usr/bin/grep -o '^### BL-[0-9]*' BACKLOG.md | sort -V | uniq -d    # must be empty
```

**Commit by pathspec** — `git commit <path> … -m "..."` — never `git add -A`, `git add .`, or a bare
`git commit` after staging. The index is shared across concurrent agents. (BL-409.)

---

# History

*Dated records of past events. Not re-verified — do not quote as current state.*

## Deploy 2026-08-02T00:36Z — `4d2773bad484` → `a4892123287b`, pid 65352 → 85177

Deliberate deploy to close the artifact drift left armed by the BL-393 incident. Verified by shasum
+ pid rotation + behaviour. This is the artifact still running at the time of writing.

`soxe service restart` printed `reaper: SIGTERM → pid 65352 (grace 5000ms)` with **no escalation
line** — the stated pass condition for BL-405's SIGTERM fix, and the previous restart had printed
`survived SIGTERM after 5000ms → SIGKILL`. The WAL nevertheless grew rather than truncating.

Two fixes verified live in the same pass: BL-404 (`role` went `"test"` → `"live-service"`) and
BL-406 (`unstamped` went 1090 → 0, with vectors untouched).

## BL-393's first live reproduction — 2026-08-02T00:15:29Z

Production restarted with nobody choosing it. The on-disk artifact had drifted to `ba49093f`, built
from **uncommitted source** containing half-finished BL-405 work.

**The trigger was identified and it was not what was first recorded.** The initial hypothesis in the
backlog named a transitive `registry:sync-index` rebuild. Subsequent investigation showed the
artifact hash was unchanged throughout and no restart appears in the audit log: what actually
happened is that **doctor-tick's routine singleton-violation self-heal SIGTERM'd a duplicate
memory-server backend (pid 56514) at 00:14:42Z**, 47 seconds before the rotation. What spawned the
duplicate has not been identified. See `handoff/bl393-respawn-trigger.md`.

## Deploy 2026-08-01 #2 — `90c7bb000580` → `5e8e1fcc8625`, pid 3040 → 18521

First artifact of the day with establishable provenance, built from a clean tree at `4f963d2`. The
previous running artifact `90c7bb000580` existed in **no file and no commit** — an intermediate
bundle the proxy respawned onto when a build deleted the file the backend was executing. Production
had been running unreproducible code for roughly two hours.

Behaviour verified post-deploy, not just liveness: a real write ran Phase A 88 ms → embed 332 ms →
apply 27 ms with zero embed failures. Two defects were found **by** that verification and filed
rather than glossed: BL-398 and BL-399.

## Deploy 2026-08-01 #1 — `6a0c13cda152` → `6d1b2abc1c12`

Six fixes in one deploy: BL-373, BL-374, BL-365, BL-324, BL-381, BL-382 (+ BL-344).

`[inv:deploy-verified]` earned its place — the documented failure reproduced live. After
`launchctl kickstart -k`, the proxy had rotated (67303 → 32395) while **backend 69947 survived,
still executing the previous bundle.** Only `kill -TERM 69947` actually deployed. Had the run
stopped at kickstart, every check an operator would plausibly run would have been green and no code
would have changed — for the third time.

BL-381 was verified by A/B on live telemetry, which is the only evidence distinguishing "fixed" from
"never ran": `no such column: k` went 113 (07-31) → 0 → 0, and `topKQuery undefined` 8 → 13 → 0.
Absence of an error line alone would have proven nothing, since the old build logged these too.

## S0–S9 — the original restoration sequence, all complete

| # | State | Evidence |
|---|---|---|
| S0 | Diagnose Turso migration failures | 90 backlog items filed, all cited |
| S1 | Recover lost `db.ts` Turso wiring | `2ad196f` — 208 lines recovered from sourcemaps |
| S2 | Adapter self-verify + repair (BL-352) | `fa786a2` — probes + repair; `integrity.ts` implements verify **and** `repairStoreIntegrity` |
| S2b | Unlinked-WAL data loss (BL-330) | `close()` re-checks WAL identity and checkpoints an orphaned WAL back into the main file — 140/140 recovered vs total loss |
| S3 | Deploy; live store self-heals (BL-347) | `fts_match('memory')` 0 → 1156, `('turso')` 0 → 138, `('backlog')` 0 → 84; duplicate `_adapter_meta` keys gone. **No manual DDL** — the adapter repaired itself, which was the owner's explicit requirement |
| S4 | Fix `ProcessType: Background` (BL-331) | pri 4 → 20 by PID; embed p50 6422 ms → 333 ms, length-matched 18.9x |
| S5 | Re-enable `SOX_DISABLE_EMBED_HEAL` (BL-339) | brake removed; 3,246 heals applied, 0 failed |
| S6 | Re-enable `SOX_DISABLE_PERIODIC_ENRICH` (BL-346) | brake removed; neither var present in the live plist |
| S7 | Drain embed backlog | 3,246 → **0**; 3,249 embeds, **0 failed** |
| S8 | Clustering research (BL-350/BL-356) | PKT-28 complete, measured on the **true full corpus N=4867 with no projection** (this supersedes the earlier N=1616 sample, which was an explicitly-labelled upper-bound trend): a fixed global τ is **not viable at any value**. τ=0.82 gives largest-cluster ratio 0.759; 0.85 is degenerate (0.514); only 0.87 holds (0.181). "Raise minPts" directly ruled out — 6× minPts moves 0.759→0.6365 while clustered_frac collapses 0.886→0.659. Replacement: target-mean-degree calibration at cluster time. **Implementation (PKT-29/PKT-30) never ran.** ⚠️ **τ=0.87 is a PAIRWISE number** — see the warning in `PLAN.md` §P0.5 before applying it to any centroid-based join; the pairwise→centroid offset measured +0.086 to +0.127, which turns a 0.87 centroid bar into ~0.78 pairwise-equivalent, well past the degenerate cliff. |
| S9 | Tracing substrate (BL-351) | `@adhd/sox-telemetry` published; `initTelemetry` wired into production (BL-404). **Consumer migration never happened** — live `stages_declared: 0`, so BL-401 stands |

## Resolved during this program — no longer live concerns

BL-330, BL-331, BL-339, BL-346, BL-347, BL-348, BL-350, BL-352, BL-364, BL-365, BL-367, BL-369,
BL-371, BL-372, BL-373, BL-374, BL-376, BL-377, BL-380, BL-381, BL-382, BL-384, BL-385, BL-386,
BL-388, BL-390, BL-395, BL-397, BL-402, BL-406, BL-407, BL-410 and the rest are in `CHANGELOG.md`.
`BACKLOG.md` holds only open work; if an id is not there, it shipped.

**BL-367 (cross-backend recall parity)** deserves a note because it was for a long time described
here as "the biggest open unknown in the project": `recall-parity.test.ts` compared `RecallResult.uid`
across two independent stores while `memoryWrite` mints a fresh `ulid()` per episode, so overlap was
**0 by construction** and the test could never have passed. It is resolved and archived — but the
lesson stands: a test that cannot pass is worse than a missing test, because it reads as coverage.
