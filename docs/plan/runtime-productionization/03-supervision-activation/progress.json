{
  "context": "03-supervision-activation",
  "items": [
    {
      "id": "SA-1",
      "bl": [],
      "title": "activation_posture config key + deriveOsUnitSpec branch",
      "status": "complete",
      "evidence": {
        "commands": [{"cmd": "npx nx build host-runtime", "exit": 0, "summary": "build passes"}, {"cmd": "npx nx test host-runtime", "exit": 0, "summary": "168/168 pass"}],
        "commits": ["eff03c1"],
        "notes": "activation_posture in config_schema; deriveOsUnitSpec branches; legacy fallback preserved"
      },
      "notes": [
        "activation_posture added to memory-server config_schema per CONTRACTS §I",
        "deriveOsUnitSpec() branches: always-on → RunAtLoad+KeepAlive; on-demand → socket-activation",
        "CONTRACTS §I keys: transports, http_port, bind_address, auth_token also added to config_schema",
        "Legacy manifest lifecycle fallback preserved for backward compatibility"
      ]
    },
    {
      "id": "SA-2",
      "bl": [],
      "title": "Sockets plist + systemd .socket rendering",
      "status": "complete",
      "evidence": {
        "commands": [
          {"cmd": "npx nx build host-runtime", "exit": 0, "summary": "build passes"},
          {"cmd": "npx nx test host-runtime", "exit": 0, "summary": "168/168 pass"},
          {"cmd": "npx nx build service-proxy", "exit": 0, "summary": "build passes"},
          {"cmd": "npx nx test service-proxy", "exit": 0, "summary": "42/42 pass"}
        ],
        "commits": ["591ff5d"],
        "notes": "LaunchdPlatform.renderBody() Sockets dict; SystemdPlatform.renderSocketUnit().socket unit; renderSocketUnit undefined when no socketPath; socket unit content-addressed (content-hash); golden-fixture tests (7) for structural order + content-hash stability + filename convention"
      },
      "notes": [
        "os-unit.ts: LaunchdPlatform Sockets dict in plist when socketPath set; SystemdPlatform.renderSocketUnit() produces standalone .socket unit with ListenStream, SocketMode, Service=, WantedBy=sockets.target",
        "os-unit.spec.ts: 7 SA-2 tests — launchd Sockets dict/omission/renderSocketUnit undefined/golden fixture stability; systemd renderSocketUnit defined/undefined/stability/filename convention",
        "Negative control: always-on posture with no socketPath omits Sockets dict entirely, preserving backward compatibility",
        "renderSocketUnit content-hash uses same unitContentHash() function as service units, ensuring [inv:os-unit-content-addressed]",
        "SA-2 committed in 591ff5d (partial); remaining socket-activation wiring completed in uncommitted work"
      ]
    },
    {
      "id": "SA-3",
      "bl": [],
      "title": "Inherited-fd serveBackend",
      "status": "complete",
      "evidence": {
        "commands": [
          {"cmd": "npx nx build service-proxy", "exit": 0, "summary": "build passes"},
          {"cmd": "npx nx test service-proxy", "exit": 0, "summary": "42/42 pass"}
        ],
        "commits": [],
        "notes": "serveBackend now accepts inheritFd option; when set, uses server.listen({fd}) instead of create+bind+chmod; close() does NOT unlink socket file (did not create it); 4 new tests in backend.spec.ts: negative control, injectFd round-trip, multiple requests, file persistence"
      },
      "notes": [
        "backend.ts: inheritFd option on ServeBackendOptions; useInheritedFd branch skips mkdir/unlink/create/chmod; server.listen({fd}) for inherited fd; close() does not unlink socket file",
        "backend.spec.ts: 4 tests — negative control (normal UDS path), inheritFd round-trip, multiple requests, behavioral verification (no file creation/unlink)",
        "Negative-control invariant: when inheritFd is omitted (the normal case), the original create+bind+chmod path is unchanged — proven by negative control test",
        "Socket fd dupping via /dev/fd/N (macOS) / /proc/self/fd/N (Linux) — works for socket fds on both platforms"
      ]
    },
    {
      "id": "SA-4",
      "bl": [
        "BL-137"
      ],
      "title": "Fallback spawn hardening: probe-before-bind, handshake, lock liveness",
      "status": "complete",
      "evidence": {
        "commands": [
          {"cmd": "npx nx build service-proxy", "exit": 0, "summary": "build passes"},
          {"cmd": "npx nx test service-proxy", "exit": 0, "summary": "48/48 pass (7 files)"},
          {"cmd": "npx nx build host-runtime", "exit": 0, "summary": "build passes"},
          {"cmd": "npx nx test host-runtime", "exit": 0, "summary": "177/177 pass — no breakage"}
        ],
        "commits": [],
        "notes": "serveBackend now probes socket before bind (E_LIVE_SOCKET on live steal). ensureBackend uses handshakeBackend (RPC ping) for readiness, monitors child exit for fast failure. 5 new tests: handshakeBackend(3), exit handling, negative control."
      },
      "notes": [
        "backend.ts: serveBackend probes socket before bind — if live, rejects with E_LIVE_SOCKET; stale socket cleaned",
        "ensure-backend.ts: handshakeBackend() sends JSON-RPC ping to verify handler readiness; spawnUnderLock tracks child 'exit' for fast-fail; double-checked locking re-probes under lock",
        "backend.spec.ts: test 5 — serveBackend refuses to bind on live socket",
        "ensure-backend.spec.ts: 4 SA-4 tests + 1 negative control — exit handling, handshake(3), unlink-first live breakage"
      ]
    },
    {
      "id": "SA-5",
      "bl": [
        "BL-121"
      ],
      "title": "Store identity stamp + E_STORE_MISMATCH",
      "status": "complete",
      "evidence": {
        "commands": [
          {"cmd": "npx nx build memory-core", "exit": 0, "summary": "build passes"},
          {"cmd": "npx nx test memory-core", "exit": 0, "summary": "191/192 pass (11 files)"},
          {"cmd": "npx nx build memory-server", "exit": 0, "summary": "build passes"},
          {"cmd": "npx nx test memory-server", "exit": 0, "summary": "84/84 pass (5 files)"}
        ],
        "commits": [],
        "notes": "openDb stamps sox_store_meta with schema_version, writer_artifact, embed_model, embed_dimensions on first open-for-write; subsequent opens verify via verifyStoreMeta (INSERT OR IGNORE ensures first-write wins); EStoreMismatch thrown on schema_version or embed_dimensions drift; embed_model difference logged as non-fatal warning. 7 new tests in db.spec.ts covering stamp, idempotency, verify, mismatch (2), model warning, and no-overwrite."
      },
      "notes": [
        "STORE_META_KEYS: schema_version, writer_artifact, embed_model, embed_dimensions — stamped at openDb via stampStoreMeta()",
        "EStoreMismatch class carries both expected and actual values for diagnostic clarity",
        "verifyStoreMeta() re-reads meta on every open-for-write; hard mismatch throws; embed_model warns via console.error",
        "setWriterArtifact/getWriterArtifact allows server to set its own identity string (e.g. memory-server@1.1.0)",
        "7 tests: stamp, idempotency, verify (no throw), schema_version mismatch, embed_dimensions mismatch, model warning, re-open no-overwrite"
      ]
    },
    {
      "id": "SA-6",
      "bl": [
        "BL-130"
      ],
      "title": "Named-store registry + resolved-path echo",
      "status": "complete",
      "evidence": {
        "commands": [
          {"cmd": "npx nx build memory-core", "exit": 0, "summary": "build passes"},
          {"cmd": "npx nx test memory-core", "exit": 0, "summary": "191/192 pass (11 files)"},
          {"cmd": "npx nx build memory-server", "exit": 0, "summary": "build passes"},
          {"cmd": "npx nx test memory-server", "exit": 0, "summary": "84/84 pass (5 files)"}
        ],
        "commits": [],
        "notes": "store-registry.ts: resolveStoreName for registry lookup; resolveStoreOrDbPath for store/db_path precedence; computeFingerprint for file identity via size:mtimeMs; [inv:store-registry-misroute] — unknown name returns E_UNKNOWN_STORE, never creates file. All tool schemas updated with store param. 11 tests in store-registry.spec.ts covering known name, unknown name, empty, precedence, deprecation, null, fingerprint, missing registry, malformed JSON."
      },
      "notes": [
        "readStoreRegistry(): reads ~/.memory/registry.json, returns {} on missing/malformed",
        "resolveStoreName(name): registry lookup — found→ResolvedStore, not found→E_UNKNOWN_STORE, never creates file",
        "resolveStoreOrDbPath(store, db_path): store wins over db_path with warning; db_path accepted with deprecation warning; null when neither",
        "computeFingerprint(filePath): ${size}:${mtimeMs} for quick identity check",
        "[inv:store-registry-misroute] enforced: unknown name → E_UNKNOWN_STORE structured error, no file created",
        "All 19 memory_* tool schemas now include 'store' param (before db_path) for consistent registry access",
        "11 tests: known name, unknown name, empty, both params, raw db_path, null, whitespace, fingerprint (existing/missing), missing registry, malformed JSON"
      ]
    },
    {
      "id": "SA-7",
      "bl": [
        "BL-122",
        "BL-131"
      ],
      "title": "memory_ping instance/store/embed identity",
      "status": "complete",
      "evidence": {
        "commands": [
          {"cmd": "npx nx build memory-core", "exit": 0, "summary": "build passes"},
          {"cmd": "npx nx test memory-core", "exit": 0, "summary": "191/192 pass (11 files)"},
          {"cmd": "npx nx build memory-server", "exit": 0, "summary": "build passes"},
          {"cmd": "npx nx test memory-server", "exit": 0, "summary": "84/84 pass (5 files)"}
        ],
        "commits": [],
        "notes": "memory_ping handler enhanced with instance block (pid, started_at, transport, instance_id), store block (name, path, fingerprint, wal_bytes, enrichment_watermark, queue_depth), and embed block (model, backend, state, on_hash_fallback, last_error). Legacy flat keys preserved for backward compatibility. store param added to ping schema. resolveStoreOrDbPath integrated into ping's store block construction."
      },
      "notes": [
        "Instance block: pid, started_at, transport, instance_id (crypto.randomUUID per boot)",
        "Store block: name, path, fingerprint (sha256), wal_bytes, enrichment_watermark, queue_depth — resolved via store param registry + db probe",
        "Embed block: model, backend, state, on_hash_fallback, last_error — from getEmbedHealth()",
        "Legacy flat keys preserved: embed_model, embed_backend_configured, embed_state, embed_on_hash_fallback, last_embed_error",
        "Content-addressed artifact identity (id, artifact, short, host_compat) unchanged",
        "resolveStoreOrDbPath integrated for store param → registry lookup in ping probe",
        "All tests pass without modification (backward compatible shape)"
      ]
    },
    {
      "id": "SA-8",
      "bl": [
        "BL-128",
        "BL-145"
      ],
      "title": "Writer lease; GATED launchd re-enable after 02",
      "status": "pending",
      "evidence": null,
      "notes": []
    }
  ],
  "gate": {
    "status": "pending",
    "evidence": null
  },
  "blockers": [],
  "discovered": [],
  "updated_at": "2026-07-03T18:50:00Z"
}
